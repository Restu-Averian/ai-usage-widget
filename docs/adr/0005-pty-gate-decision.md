# ADR 0005 — Gate Interactive or Text Parsing Behind Evidence

**Status:** Accepted

Prefer official structured local interfaces. Use version-aware text or interactive parsing only when necessary, with sanitized fixtures, timeouts, output limits, and fail-closed behavior.

Never use browser cookies, raw provider credential files, or private network interception.
